import styles from './styles.module.scss'

const Footer = () => {
    return (
        <footer className={styles.footer}>
            <p>Made by Yurii Salash &copy;</p>
        </footer>
    );
};

export default Footer;